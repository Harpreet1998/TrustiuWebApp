@model IEnumerable<TrustiuWebApp.Models.SubCategory>

@{
    ViewData["Title"] = "Index";
}

<div class="text-center">
    <h1 class="display-4">Product Sub Category Operations</h1>
    <p>View All Product Sub Category</p>
</div>
<p>
    <a asp-action="Create" class="btn btn-dark">Add New Product Sub Category</a>
</p>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SubCategoryName)
            </th>
            <th>
                Category Name
            </th>
            <th colspan="2">Operations</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SubCategoryName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Category.CategoryName)
                </td>
                <td>
                    <a asp-action="Edit" class="btn btn-info" asp-route-id="@item.SubCategoryID">Edit</a>
                </td>
                <td>
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.SubCategoryID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
